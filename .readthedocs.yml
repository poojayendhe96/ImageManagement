version: 2

# Build configuration
build:
  image: latest

# Documentation versions
versions:
  latest:
    name: Latest
    identifier: main
    default: true

# Build instructions
  script:
    - mkdocs build

